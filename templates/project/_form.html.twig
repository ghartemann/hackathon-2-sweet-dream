{{ form_start(form) }}
    {{ form_widget(form, {
        "attr": {
            "class": "text-white",}
    }) }}
<div class="d-flex justify-content-center">
    <button class="btn btn-secondary mt-3">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
